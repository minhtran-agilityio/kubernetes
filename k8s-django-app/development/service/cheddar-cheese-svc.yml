apiVersion: v1
kind: Service
metadata:
  labels:
    app: cheddar-cheese-deployment
    kubernetes.io/minikube-addons: ingress
    kubernetes.io/minikube-addons-endpoint: ingress
    addonmanager.kubernetes.io/mode: Reconcile
  name: cheddar-cheese-deployment
  namespace: k8s-django-app-development
spec:
  ports:
  - nodePort: 32444
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: cheddar-cheese-deployment
  type: NodePort
